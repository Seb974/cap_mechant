{% extends 'base.html.twig' %}

{% block title %}Metadata{% endblock %}

{% block body %}
    <h1>Metadata</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metadata.id }}</td>
            </tr>
            <tr>
                <th>Facturation_address</th>
                <td>{{ metadata.facturationAddress }}</td>
            </tr>
            <tr>
                <th>Delivery_address</th>
                <td>{{ metadata.deliveryAddress }}</td>
            </tr>
            <tr>
                <th>Phone_number</th>
                <td>{{ metadata.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('metadata_index') }}">back to list</a>

    <a href="{{ path('metadata_edit', {'id': metadata.id}) }}">edit</a>

    {{ include('metadata/_delete_form.html.twig') }}
{% endblock %}
